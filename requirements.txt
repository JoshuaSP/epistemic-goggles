# Training + rollout precompute. Versions are the ones the paper's runs used;
# nearby versions likely work but transformers >= 5.x has changed generate()
# internals before — pin if you hit issues.
torch==2.5.1
transformers==5.9.0
peft==0.19.0
accelerate
wandb
huggingface_hub  # `hf` CLI used by scripts/download_data.sh

# Capability suite only (eval/README.md):
#   pip install vllm inspect-ai inspect-evals
