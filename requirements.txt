# Training + rollout precompute. Versions validated to run this release
# end-to-end; nearby versions likely work. Note: peft 0.19 requires torch >= 2.7
# (it references torch.float8_e8m0fnu), and transformers 5.x changed generate()
# internals — pin if you hit issues.
torch==2.12.0
transformers==5.10.2
peft==0.19.0
accelerate
wandb
huggingface_hub  # `hf` CLI used by scripts/download_data.sh

# Capability suite only (eval/README.md):
#   pip install vllm inspect-ai inspect-evals
# Per-question rubric judge only (eval/rubric_judge.py; external LLM, needs OPENAI_API_KEY):
#   pip install openai
